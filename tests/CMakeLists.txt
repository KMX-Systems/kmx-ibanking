find_package(Qt6 REQUIRED COMPONENTS Test)

function(kmx_add_test name)
    qt_add_executable(tst_${name} tst_${name}.cpp)
    target_compile_definitions(tst_${name} PRIVATE KMX_TEST_HOOKS)
    target_link_libraries(tst_${name} PRIVATE kmxcore Qt6::Test)
    add_test(NAME ${name} COMMAND tst_${name})
    set_tests_properties(${name} PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
endfunction()

kmx_add_test(money)
kmx_add_test(seed_generator)
kmx_add_test(connection_service)
kmx_add_test(auth_service)
kmx_add_test(aggregation)
kmx_add_test(fx_service)
kmx_add_test(ledger_filters)
kmx_add_test(payments)
kmx_add_test(exchange_advisor)
kmx_add_test(cards)
kmx_add_test(analytics)
kmx_add_test(qml_surface)
